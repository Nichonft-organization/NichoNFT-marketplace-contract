{
  "name": "nichonft_marketplace_contract",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "testnet": "truffle migrate --reset -f 1 --to 1 --network testnet",
    "testnet-auction": "truffle migrate --reset -f 2 --to 2 --network testnet",
    "verify-testnet": "truffle run verify NichoNFT --network testnet && truffle run verify Nicho --network testnet && truffle run verify NFTBlackList --network testnet && truffle run verify NichoNFTMarketplace --network testnet",
    "verify-auction": "truffle run verify NichoAuction --network testnet",
    "mainnet": "truffle migrate --reset -f 1 --to 1 --network mainnet",
    "mainnet-auction": "truffle migrate --reset -f 2 --to 2 --network mainnet",
    "verify-mainnet": "truffle run verify NichoNFT --network mainnet && truffle run verify NichoAuction --network mainnet"
  },
  "keywords": [],
  "author": "Cui",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "@truffle/hdwallet-provider": "^2.0.15",
    "dotenv": "^16.0.2",
    "truffle-plugin-verify": "^0.5.28"
  }
}
